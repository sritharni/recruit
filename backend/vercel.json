{
  "version": 2,
  "buildCommand": "npm run build",
  "builds": [
    {
      "src": "dist/app.vercel.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/app.vercel.js",
      "methods": ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"]
    }
  ]
}
